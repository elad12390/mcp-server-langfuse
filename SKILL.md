---
name: "@elad12390/mcp-server-langfuse"
description: |
  MCP server for Langfuse prompt management. List, get, edit, publish, diff, compare versions, rollback, validate, and search prompts. Triggers: langfuse, prompt management, prompt versions, prompt diff, llm prompts.

  Trigger phrases: "langfuse", "prompt management", "prompt versions", "prompt diff", "llm prompts", "prompt rollback"
---

# @elad12390/mcp-server-langfuse

## Summary

Manages Langfuse prompts: list, get, edit, publish, diff, rollback, validate, search.

## Installation

```bash
tank install @elad12390/mcp-server-langfuse
```

See the package README for setup, environment variables, and tool reference.
